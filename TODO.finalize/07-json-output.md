# JSON output mode (deferred)

`--format json` for machine-readable output from `list`, `verify`.
Requires serde + serde_json deps and a typed output schema.
Deferred until the CLI surface stabilises (list/verify land first).
