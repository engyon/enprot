# `enprot list` subcommand

Parse-only listing of all WORD segments in a file: keyword, type
(BEGIN/END, ENCRYPTED, STORED), line range, cipher/pbkdf info.

Files: src/lib.rs, src/etree/mod.rs, tests/cli/list.rs.
