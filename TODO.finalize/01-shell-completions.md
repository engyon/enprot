# Shell completions

Add `clap_complete` to generate bash/zsh/fish/PowerShell completion scripts.

New subcommand: `enprot completions <SHELL>` prints the script to stdout.

Files: Cargo.toml, src/lib.rs.
